name: test-crypto-primitives

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Build crypto-primitives package
        run: npm run --workspace @meproto/crypto-primitives build

      - name: Test crypto-primitives package
        run: npm run --workspace @meproto/crypto-primitives test